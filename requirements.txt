Flask==2.3.3
# Upgrade pandas for Python 3.13 wheel availability
pandas==2.2.3
# Explicit numpy pin with Py 3.13 wheels
numpy==2.1.1
gunicorn==21.2.0
requests==2.31.0
unidecode==1.3.6
rapidfuzz==3.2.0
# Removed scipy (no longer needed after ratings feature removal). Re-add if needed.
# psutil removed (only used for local debug endpoint /_whoami)
